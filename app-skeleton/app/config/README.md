# app/config

Reads environment variables from `.env` (see `config/.env.example`), and matching configuration (`matching_config_v1`, etc. -- see Technical Design Document Section 16; configs must be versioned).
