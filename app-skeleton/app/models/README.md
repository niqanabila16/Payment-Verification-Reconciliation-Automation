# app/models

SQLAlchemy models that mirror `database/schema.sql`. If you add a new column/table, update BOTH (schema.sql and the model here) plus write a migration script (see the Definition of Done notes in Solo Delivery Plan Section 8).

See `database/ERD.md` for an explanation of each entity and its relationships before adding a new model here.
