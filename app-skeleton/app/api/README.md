# app/api

FastAPI routers -- HTTP endpoints (see Technical Design Document Section 23 for the planned endpoint list: `/transactions`, `/receipts`, `/bank-files`, `/reconciliations`, `/reports`, `/audit-logs`).

Start here if you're opening this project for the first time and want to know "what endpoints are available."
