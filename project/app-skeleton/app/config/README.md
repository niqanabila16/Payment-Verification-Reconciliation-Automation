# app/config

Baca environment variable dari `.env` (lihat `config/.env.example`), dan konfigurasi matching (`matching_config_v1`, dst -- lihat Technical Design Document Bagian 16, config harus versioned).
