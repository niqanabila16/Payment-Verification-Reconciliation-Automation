# app/api

FastAPI routers -- endpoint HTTP (lihat Technical Design Document Bagian 23 untuk daftar endpoint yang direncanakan: `/transactions`, `/receipts`, `/bank-files`, `/reconciliations`, `/reports`, `/audit-logs`).

Mulai dari sini kalau Anda baru pertama kali membuka project ini dan ingin tahu "endpoint apa saja yang tersedia".
